export default function changeVisibilityByDate(dueDate){
    let todosList = document.querySelectorAll('.todo');
    todosList.forEach(todo=>{
        let date = todo.querySelector('.todo-footer').querySelector('p');
        
    })
}